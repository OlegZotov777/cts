# CTS Магазин - Интернет-магазин расходных материалов

Современный e-commerce проект, построенный на Next.js (frontend) и NestJS (backend) с Docker.

## 🚀 Технологии

### Frontend
- **Next.js 15** - React фреймворк с SSR/SSG
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Lucide React** - Иконки

### Backend
- **NestJS** - Node.js фреймворк
- **TypeORM** - ORM для работы с БД
- **PostgreSQL** - База данных
- **JWT** - Аутентификация
- **Swagger** - API документация

### Инфраструктура
- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация

## 📁 Структура проекта

```
├── frontend/          # Next.js приложение
│   ├── src/
│   │   ├── app/       # App Router страницы
│   │   ├── components/ # React компоненты
│   │   ├── hooks/     # React хуки
│   │   ├── lib/       # Утилиты
│   │   └── types/     # TypeScript типы
│   └── Dockerfile
├── backend/           # NestJS приложение
│   ├── src/
│   │   ├── auth/      # Аутентификация
│   │   ├── products/  # Продукты
│   │   ├── categories/# Категории
│   │   ├── orders/    # Заказы
│   │   └── users/     # Пользователи
│   └── Dockerfile
└── docker-compose.yml # Docker Compose конфигурация
```

## 🛠 Установка и запуск

### С Docker (рекомендуется)

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка логов
docker-compose logs -f
```

После запуска:
- Frontend: http://localhost:3001
- Backend API: http://localhost:3000
- API Docs: http://localhost:3000/api/docs
- Adminer (DB): http://localhost:8080

### Без Docker (для разработки)

#### Backend
```bash
cd backend
npm install
cp .env.example .env
# Настройте .env файл
npm run start:dev
```

#### Frontend
```bash
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

## 🔐 API Endpoints

### Auth
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Products
- `GET /api/products` - Список продуктов
- `GET /api/products/:id` - Детали продукта
- `POST /api/products` - Создание (admin)
- `PUT /api/products/:id` - Обновление (admin)
- `DELETE /api/products/:id` - Удаление (admin)

### Categories
- `GET /api/categories` - Список категорий
- `GET /api/categories/:id` - Детали категории

### Orders
- `GET /api/orders` - Все заказы (admin)
- `GET /api/orders/my` - Заказы пользователя
- `POST /api/orders` - Создание заказа
- `PUT /api/orders/:id/status` - Изменение статуса (admin)

### Users
- `GET /api/users/me` - Профиль пользователя
- `PUT /api/users/me` - Обновление профиля

## 👥 Роли пользователей

- **user** - Обычный пользователь (розничные цены)
- **dealer** - Дилер (оптовые цены)
- **admin** - Администратор (полный доступ)

## 🎨 Особенности

- Современный адаптивный дизайн
- Корзина с сохранением в localStorage
- Разные цены для дилеров и обычных пользователей
- Поиск и фильтрация товаров
- История заказов
- JWT аутентификация
- Swagger документация API

## 📝 Лицензия

MIT